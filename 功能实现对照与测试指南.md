# 毕业设计功能实现对照与测试指南

本文档对照毕业论文要求的「语法错误检测 → 代码规范校验 → 安全漏洞识别 → 性能优化建议 → 重构方案生成」全流程，说明**已实现内容**及**如何在新打开的扩展开发主机窗口里测试**。

**重要**：插件已改为**自动化五阶段流水线**，无需人工逐项检测。运行一次「全流程审查」即依次执行：Tree-sitter 语法检测 → Pylint/ESLint 规范校验 → 内置安全规则 + Linter 高风险 → 内置性能规则 → 内置重构规则；可选再调用 CodeLlama 增强。**即使未安装 Pylint/ESLint、未配置 AI，也会产出安全/性能/重构建议**（来自 `builtinRules.ts`）。

**选择文件并审查**：可通过命令「Smart Code Review: 选择文件并全流程审查」从工作区选择任意 .py / .js / .ts 文件，自动打开并执行全流程审查，**返回报错**（问题面板 + 侧边栏 + 输出通道）。  
**修复后代码**：审查完成后，运行「Smart Code Review: 生成修复后代码」可生成**修改后的正确代码**（安全项如 eval→ast.literal_eval、风险注释，以及规范 noqa/eslint-disable 等），并以**与原文件对比的 diff 视图**或新文档展示，便于复制或另存。

---

## 一、实现情况对照

| 毕业论文要求 | 是否实现 | 对应实现位置 |
|-------------|----------|--------------|
| **1. 代码解析引擎与数据集** | ✅ | Tree-sitter 解析：`treeSitterService.ts`、`astExtract.ts`；支持 Python/JavaScript/TypeScript；Java 通过 Checkstyle 做规范校验。数据集目录与标注说明：`datasets/README.md` |
| **2. 规则引擎与第三方工具** | ✅ | `lintRunner.ts`：Pylint（Python）、ESLint（JS/TS）、Checkstyle（Java）；统一 `RuleIssue`；与解析结果联动（全流程中先解析再规则再 AI） |
| **3. CodeLlama 模型集成与高级功能** | ✅ | `aiClient.ts`：HTTP 调用、输入裁剪、结果解析；分类含 security/performance/refactor；提示词与接口见 `docs/codellama-integration.md` |
| **4. 全流程与插件界面** | ✅ | `reviewCoordinator.runFullReview`：解析→规则→（可选）AI→合并；侧边栏「审查结果」按语法/规范/安全/性能/重构展示；Problems 面板、CodeAction 快速修复 |
| **语法错误检测** | ✅ | Tree-sitter 解析报错 → `parseResult.errors` → 写入 Diagnostics 与侧边栏 |
| **代码规范校验** | ✅ | Pylint/ESLint/Checkstyle 输出 → `RuleIssue[]` → Diagnostics + 侧边栏「规范/style」 |
| **安全漏洞识别** | ✅ | **内置规则**（`builtinRules.runSecurityRules`）：eval/exec、pickle.loads、os.system、SQL 拼接、XSS 等；Linter 高风险也归入此类；AI 可补充 |
| **性能优化建议** | ✅ | **内置规则**（`builtinRules.runPerformanceRules`）：循环内 len/append、in 列表改 set、字符串拼接等；AI 可补充 |
| **重构方案生成** | ✅ | **内置规则**（`builtinRules.runRefactorRules`）：长函数、重复代码块；AI 可补充 |

---

## 二、测试前准备

- **操作位置**：在 **F5 弹出的新窗口（扩展开发主机）** 里进行所有测试，不要在原 VS Code 窗口。
- **可选环境**：  
  - 测 **Python 规范/安全**：本机安装 Pylint（`py -m pip install pylint`）。  
  - 测 **JavaScript/TypeScript 规范**：项目或全局安装 ESLint（`npm install -g eslint` 或项目内 eslint）。  
  - 测 **AI 功能**（安全/性能/重构）：需先部署符合接口的 CodeLlama 服务并配置 `smartCodeReview.ai.serverUrl`；未配置时仅规则与语法检测可用，AI 会提示连接失败。

---

## 三、分项功能测试步骤

### 1. 语法错误检测

**目的**：验证 Tree-sitter 解析出的语法错误能出现在「问题」面板和审查结果中。

1. 在扩展开发主机窗口：**文件 → 打开文件夹**，选一个任意空文件夹或你的项目。
2. 新建或打开一个 **Python** 或 **JavaScript** 文件（如 `test.py`）。
3. 写入一段**故意带语法错误**的代码，例如 Python：

   ```python
   def foo(
       print("missing closing paren"
   ```

   或 JavaScript：

   ```javascript
   function bar( {
     console.log("missing closing paren");
   }
   ```

4. **Ctrl+Shift+P** → 输入并选择 **「Smart Code Review: 全流程审查」**。
5. **预期**：  
   - 底部 **「问题」** 面板出现语法相关错误；  
   - 左侧 **Smart Code Review → 审查结果** 中，该文件下应有「语法」或对应错误条目；  
   - 输出通道 **Smart Code Review** 中有 `[1/4] Tree-sitter 语法解析` 及错误信息。

---

### 2. 代码规范校验（Pylint / ESLint）

**目的**：验证 Pylint（Python）或 ESLint（JS/TS）的规范问题能显示在问题面板和侧边栏。

**Python 示例（需已安装 Pylint）**  
在 `.py` 文件中写：

```python
x = 1
y = 2
def f():
    z = 3
    return z
```

- 未使用的变量、命名风格等可能被 Pylint 报出。  
- 执行 **全流程审查**，在 **问题** 和 **审查结果** 中应看到 `[pylint]` 开头的规范类问题。

**JavaScript 示例（需项目或全局有 ESLint）**  
在 `.js` 文件中写：

```javascript
var a = 1;
function f() {
  var b = 2;
  return b;
}
```

- 执行 **全流程审查**，在 **问题** 和 **审查结果** 中应看到 `[eslint]` 开头的规范类问题。

---

### 3. 安全漏洞识别（规则侧：高风险标记）

**目的**：验证使用 `eval`/`exec` 等时被标记为高风险，并在问题与侧边栏中体现。

在 Python 文件中写：

```python
def bad():
    eval("1+1")
```

- 执行 **全流程审查**。  
- **预期**：问题面板或侧边栏中出现与 `eval` 相关的 Pylint 规则（如 eval-used），且插件内部会将其视为 highRisk（可配置为仅此时触发 AI）。

---

### 4. 性能优化建议与重构方案（AI 功能）

**目的**：验证 AI 返回的「性能」「重构」类建议能出现在侧边栏和输出中。

- **前提**：已配置并启动 CodeLlama 服务（如 `http://localhost:8000/v1/code-review`），且接口返回格式符合 README 中的「AI 服务接口约定」。
1. 打开一段稍复杂的代码（如含循环、重复逻辑的函数）。
2. **Ctrl+Shift+P** → **「Smart Code Review: 全流程审查」** 或 **「Smart Code Review: 智能分析（仅模型）」**。
3. **预期**：  
   - 输出通道中有 AI 的 summary 及分类建议；  
   - 侧边栏 **审查结果** 中该文件下出现 **性能**、**重构** 分类及具体条目；  
   - 若 AI 返回了 `exampleCode`，在侧边栏条目上悬停可看到示例代码。

若未配置 AI 服务：执行全流程审查会照常完成语法+规则部分，输出中会提示 AI 连接失败，不影响前几项测试。

---

### 5. 全流程串联测试

**目的**：一次操作走通「语法错误检测 → 代码规范校验 →（可选）安全/性能/重构」全流程。

1. 用同一文件或新建文件，同时包含：  
   - 一处语法错误；  
   - 若干规范问题（如未使用变量）；  
   - （可选）安全相关代码（如 eval）；  
   - 若已接 AI，可写稍复杂逻辑以触发性能/重构建议。
2. 执行 **「Smart Code Review: 全流程审查」**。
3. **预期**：  
   - 输出通道依次出现：`[1/4] Tree-sitter 语法解析`、`[2/4] 规则校验`、`[3/4] 调用 AI...` 或跳过、`[4/4] 合并结果`。  
   - **问题** 面板：语法 + 规则问题都有。  
   - **审查结果** 侧边栏：按文件 → 按类别（语法/规范/安全/性能/重构）展示，点击条目可定位到行。

---

### 6. 侧边栏与定位到行

- 执行过至少一次全流程审查后，点击左侧活动栏 **Smart Code Review** 图标，展开 **审查结果**。  
- 按 **文件 → 类别 → 具体条目** 查看；点击条目应跳转到对应文件行。  
- 若条目带示例代码，悬停可看 tooltip。

---

### 7. CodeAction 快速修复（可选）

**目的**：验证对部分规范问题（如未使用变量）提供一键修复。

- 在 Python 中写未使用变量（如 `a = 1` 且未使用 `a`），或在 JS 中写未使用变量，触发 Pylint/ESLint 对应规则。  
- 在该行或问题位置点击灯泡或 **Ctrl+.**，查看是否有 **Smart Code Review** 提供的快速修复（如忽略本行）。  
- 若有则选择应用，确认问题被忽略或修复。

---

## 四、测试结果记录建议（用于论文/答辩）

可按下表整理，便于写「功能准确性测试」与「实验结果」：

| 测试项           | 测试用例简述           | 预期结果                     | 实际结果 |
|------------------|------------------------|------------------------------|----------|
| 语法错误检测     | 故意缺少括号的代码     | 问题面板 + 侧边栏出现语法错误 |          |
| 代码规范校验     | 未使用变量/命名不规范  | 出现 Pylint/ESLint 规则提示   |          |
| 安全漏洞识别     | 使用 eval 的代码       | 出现安全相关规则/高风险标记   |          |
| 性能/重构（AI）  | 复杂函数 + AI 服务开启 | 侧边栏出现性能、重构建议      |          |
| 全流程            | 综合上述多类问题       | 输出 1–4 步、问题与侧边栏一致  |          |

---

## 五、选择文件 → 报错 → 修复后代码（推荐流程）

1. **Ctrl+Shift+P** → **「Smart Code Review: 选择文件并全流程审查」**。
2. 在弹窗中选一个 **.py** 或 **.js/.ts** 文件（可浏览工作区），确定后插件会打开该文件并自动执行全流程审查。
3. **查看报错**：在 **问题** 面板、左侧 **Smart Code Review → 审查结果**、以及底部 **输出 → Smart Code Review** 中查看语法/规范/安全/性能/重构等报错与建议。
4. **生成修改后的正确代码**：**Ctrl+Shift+P** → **「Smart Code Review: 生成修复后代码」**。插件会根据审查结果生成修复后源码（如 eval→ast.literal_eval、风险行注释、noqa 等），并**以与原文件的 diff 对比视图**打开，便于对照或另存为新文件。

---

## 六、常用命令速查（在扩展开发主机窗口）

| 操作 | 方式 |
|------|------|
| **选择文件并审查（推荐）** | **Ctrl+Shift+P** → 「Smart Code Review: 选择文件并全流程审查」 |
| 全流程审查（当前打开文件） | **Ctrl+Shift+P** → 「Smart Code Review: 全流程审查」 |
| **生成修复后代码** | **Ctrl+Shift+P** → 「Smart Code Review: 生成修复后代码」 |
| 仅 AI 智能分析（当前函数/区域） | **Ctrl+Shift+P** → 「Smart Code Review: 智能分析（仅模型）」 |
| 打开审查结果侧边栏 | **Ctrl+Shift+P** → 「Smart Code Review: 显示审查结果面板」或点击左侧 Smart Code Review 图标 |
| 查看详细输出日志 | 底部 **输出** 面板，下拉选 **Smart Code Review** |

按上述步骤即可系统验证毕业设计要求的各项功能是否按预期工作。
