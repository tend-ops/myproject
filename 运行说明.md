# Smart Code Review 运行说明

## 重要：用 VS Code 打开的必须是插件目录

请用 VS Code **只打开插件这一层文件夹**，不要打开上一层的 `Graduation_design`：

- **正确**：文件 → 打开文件夹 → 选择 **`D:\Graduation_design\smart-code-review-vscode`**
- **错误**：打开 `D:\Graduation_design`（这样 F5 会找不到扩展）

---

## 侧边栏在哪里？

审查结果在**单独的视图**里，不在左侧「资源管理器」下面：

1. 看编辑器**最左侧竖条**（活动栏），从上到下有很多图标。
2. 找到 **「Smart Code Review」** 图标（或带放大镜/代码的图标），**点击它**。
3. 左侧会切换成 **Smart Code Review** 面板，下面有 **「审查结果」** 树。
4. 运行一次 **「全流程审查」** 后，这里会自动打开并显示结果；若没有结果会显示「请先打开 .py / .js / .ts 文件并运行全流程审查」或「未发现问题」的提示。

---

## 方式一：先编译再按 F5（推荐）

1. 用 VS Code 打开文件夹：**`smart-code-review-vscode`**
2. 按 **Ctrl+`** 打开终端，在终端里执行：
   ```bash
   npm run compile
   ```
   看到没有报错即可。
3. 按 **Ctrl+Shift+D** 打开“运行和调试”，在顶部下拉框选择 **「运行扩展（推荐：先编译再运行）」**。
4. 按 **F5**（或点绿色三角）。
5. 会先自动再执行一次编译，然后弹出一个新窗口（**[扩展开发主机]**），在新窗口里打开你的代码文件，用命令面板（Ctrl+Shift+P）运行 **Smart Code Review: 全流程审查** 即可。

---

## 方式二：不自动编译，完全手动

1. 用 VS Code 打开文件夹：**`smart-code-review-vscode`**
2. 终端执行：
   ```bash
   npm install
   npm run compile
   ```
3. 在“运行和调试”里选择 **「运行扩展（无预编译，需先手动 npm run compile）」**，按 **F5**。
4. 之后如果改了 `src` 里的代码，需要再执行一次 `npm run compile` 再 F5。

---

## 你电脑已有 Python 时

- **运行插件本身**：不需要 Python，只要 Node.js 和 `npm run compile` 成功即可。
- **用 Pylint 审查 Python 代码**：需要本机能执行 `pylint`。在终端执行：
  ```bash
  py -m pip install pylint
  ```
  或
  ```bash
  python -m pip install pylint
  ```
  若你的 Python 不在 PATH 里，只在 VS Code 里用“用 Python 解释器”选好解释器后，在设置里把 `smartCodeReview.lint.pythonPath` 填成该解释器的完整路径（例如 `C:\...\python.exe`）。

---

## 若仍然无法运行

请把下面信息发出来方便排查：

1. 你当前在 VS Code 里打开的文件夹路径是？（应是 `...\smart-code-review-vscode`）
2. 按 F5 后具体现象：没反应、报错弹窗、还是新窗口闪退？若有报错，请复制完整错误内容。
3. 在终端执行 `node -v` 和 `npm run compile` 的输出（是否成功）。
